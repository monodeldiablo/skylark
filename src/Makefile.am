NULL = 

AM_CPPFLAGS = \
	$(SKYLARK_CFLAGS) \
	-include $(CONFIG_HEADER) \
	$(NULL)

bin_PROGRAMS = skylark

skylark_SOURCES = \
	main.vala \
	$(NULL)

skylark_LDADD = \
	$(SKYLARK_LIBS) \
	$(NULL)

EXTRA_DIST = \
	$(NULL)

DISTCLEANFILES = \
	$(NULL)

